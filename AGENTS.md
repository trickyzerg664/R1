# Codex 工作约束

## 沟通与表达

- 所有回答、总结和说明必须清晰、简洁、直接。
- 首先给出结论或当前结果，再补充必要依据。
- 删除寒暄、套话、重复总结和低信息量的过渡句。
- 禁止使用“不是……而是……”“不完全是……”以及类似的绕弯表达。直接陈述准确结论。
- 禁止使用黑话、生僻词和不必要的专业术语。
- 禁止英文直译的低频词，必须考虑用词的合理性。如避免使用“合同”, “契约"等不好理解的词汇
- 必须使用专业术语时，在首次出现处用常用语言简短解释。
- 工作汇报只保留以下内容：
  - 完成结果
  - 关键修改
  - 验证结果
  - 阻塞项或需要用户决定的事项

## 仓库整洁要求

- 代码库保持有序管理. 禁止在项目目录下堆叠文件。须按需创建目录/子目录/文件。

## 实验代码设计约束

- 新增或修改实验代码时必须保持低耦合、高内聚：同一职责的实现集中在独立模块中，不把评分、采样、状态恢复和结果汇总散落到训练主循环。
- 实验模块通过明确的配置、数据结构和少量调用接口接入训练器；未启用实验功能时，原训练流程和默认行为应保持兼容。
- 核心算法与 Ray、GPU、文件路径等运行环境分离；评分和训练复用已有生成、检索及奖励实现，避免复制出两套行为不同的流程。
- 模块依赖保持单向，优先组合；禁止依赖训练器的整个内部状态、循环导入、运行时 monkey patch 或跨模块修改私有字段来接入实验功能。
- 配置、标签、采样器和 checkpoint 状态的版本及校验集中管理；恢复策略和允许变更的字段必须显式定义。
- 每个修改的代码块应有中文注释，说明修改原因、输入输出或关键约束；新增模块的接口也应有清晰说明。
- 对采样、评分记录和状态恢复编写可独立运行的 CPU 测试；完整 GPU 联调结果单独记录，不以模拟测试代替。

### 实验代码修改检查

- 修改前明确模块职责、输入输出和依赖方向；新增功能优先放入对应实验子包，训练器及 worker 仅保留必要的适配调用。
- 共用训练、生成、检索和奖励代码中的修改必须说明复用原因，并验证实验关闭时的兼容性；禁止将具体实验组名、配比或本机路径写入共用代码。
- 提交或交付前检查职责是否集中、接口是否最小、是否存在重复实现及隐式共享状态，并在实验实现文档中分别记录已实现、未接入和已验证的内容。

## 实验进度与 agent 交接

- **每次改代码或跑实验后必须同步更新相关文档，作为本次工作的完成条件，不得等用户提醒或留到最终汇总。** 一次逻辑修改完成后、切换任务或交接前都应同步；实验失败、中断、仅完成部分步骤时同样适用。
- 改代码后更新 `docs/experiments/data_difficulty_code_changes.md` 和进度台账：记录修改文件、功能或行为变化、验证命令及结果、尚未验证的内容和下一步。未运行测试要明确写明，不能沿用旧测试结果为新代码背书。
- 跑实验后更新对应 `runs/<run_id>.md` 和进度台账；若改变方案、参数、步骤或运行条件，同时更新方案、步骤或条件文档。正在运行的长任务按已确认的日志进度同步，不等待任务全部结束。

- 难度实验接手时先读 `docs/experiments/data_difficulty_progress.md`，再读对应方案及单次运行记录；不得只依赖聊天历史推断进度。
- 设计或修改方案后同步更新执行步骤、验收标准和进度台账。每次开始、达到阶段节点、保存 checkpoint、失败、暂停、恢复及结束时更新对应记录；长任务每次检查进度时同步最新已确认步骤及检查时间。
- 单次运行使用唯一 run_id，按 `docs/experiments/runs/TEMPLATE.md` 建档，并在总台账登记。记录配置、代码版本及未提交修改快照、设备、命令、日志、产物、当前步骤、可恢复步骤、阻塞项和下一步。
- 明确区分方案步骤、已完成训练步数、已持久化 checkpoint 步数及评分题数；有日志或产物证据才标记完成。CPU 模拟测试、代码初稿和正式 GPU 实验分别记录。
- 交接前更新台账及运行记录；没有新进展也应说明原因。保留失败尝试和变更历史，不覆盖旧实验结论，不将计划值写成实测值。
- 下一位 agent 先检查进程、日志和 checkpoint 完整性，确认已有任务状态后再启动或恢复，避免重复运行。文档中的运行状态仅代表注明时间的检查结果。

## 注释质量与覆盖检查

- 新增实验模块提高关键逻辑的注释覆盖，按职责、公共接口、实验假设、状态切换、恢复边界和异常路径检查；不使用注释行数占比，也不为凑比例重复解释简单语句。
- 用户要求的至少 40% 覆盖按关键逻辑单元检查，在实现文档列出检查范围和依据；涉及分桶语义、随机流、训练续接和对照公平性的关键单元必须有解释。
- 中文注释优先说明为什么这样实现、输入输出约束和失败后如何处理；注释与当前实现同时修改，避免误导接手 agent。
