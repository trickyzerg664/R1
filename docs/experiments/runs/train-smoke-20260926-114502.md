# 目标机烟测：train-smoke-20260926-114502

## 当前状态

- run_id／实验组：`train-smoke-20260926-114502`，目标机两卡 5 step 烟测；由用户执行，Codex 依据用户粘贴日志记录，未直接登录目标机。
- 状态／最后检查时间：2026-09-26 11:58 UTC，失败于 step 1 的 actor 参数更新。
- 已完成训练 global step／目标绝对 step：0／5。日志有 `epoch 0, step 1`，但更新没有完成。
- 已完成评分题数：正式难度评分 0；初始验证产生输出，但有效计分题数未核实。
- 最新完整 checkpoint：日志无完成证据；目标机文件未检查，不能确认可恢复步骤。
- 下一步：同步 actor 更新元数据修复，使用新 run_id 复跑；验收为 5 个更新步骤完成、checkpoint 完整并通过恢复检查。

## 可复现信息

- 分支／代码：目标机 `data-difficulty`；日志显示已越过旧策略概率计算，实际提交及 dirty 状态未提供，未核实。
- 设备：`test-SYS-420GP-TNR`，8×RTX A6000 中选择 GPU 1、2；`NCCL_P2P_DISABLE=1`。实际进程和占卡状态未再次查询。
- 模型及数据：Qwen2.5-3B；`runs/smoke-input/train.parquet` 和 `dev.parquet`；检索服务 `http://127.0.0.1:8008/retrieve`，检索 ID `wiki18-e5-pinned`。
- 配置：`difficulty_grpo`，train/val batch=2，mini batch=8，micro batch=2，最大 5 step，测试和保存频率=5，vLLM 显存份额=0.35；完整 CLI 见用户粘贴日志。本次 shell 环境变量赋值及配置文件哈希未核实。
- 产物：`/home1/wsh/home/projects/lzy/data/search-r1/runs/train-smoke-20260926-114502/train`；用户终端通过 `tee "$RUN/train.log"` 记录，目标机日志文件未直接读取。
- 父 checkpoint／标签／题池哈希：本次烟测未提供；正式 A/B 实验尚未开始。

## 阶段进度与证据

| 检查时间 UTC | 动作 | 实测进度 | 证据与下一步 |
| --- | --- | --- | --- |
| 2026-09-26 11:58 | 模型初始化、初始验证与 step 1 搜索生成 | 已越过旧策略概率计算和奖励计算；训练更新 0 step | 用户粘贴日志，下一步 actor 更新 |
| 2026-09-26 11:58 | actor 参数更新 | `dp_actor.update_policy` 读取 `data.meta_info['temperature']` 抛 `KeyError`；训练终止 | 同步本次 worker 修复后新目录复跑 |

## 失败、变更与交接

搜索路径生成的批次缺少 `temperature` 元数据。上次修复只补齐 `compute_log_prob` 内部收到的数据；该入口返回的是概率张量，元数据不会回传给训练 driver。当前修复在 FSDP worker 的 `update_actor` 入口按 rollout 配置设置温度，与常规生成路径的配置一致。源端仅有代码修复和 CPU 检查，目标机 GPU 重试及 checkpoint 验证待执行。重试前先核实旧进程已结束，检索服务仍监听 8008，GPU 和输出目录可用。
