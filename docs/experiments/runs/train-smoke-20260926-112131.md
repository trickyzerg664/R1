# 目标机烟测：train-smoke-20260926-112131

## 当前状态

- 负责人或 agent 标识：目标机由用户执行；本记录由 Codex 据用户粘贴日志整理，目标机文件未直接检查。
- 状态／最后检查时间：2026-09-26；失败于 step 1 旧策略 log probability 计算。
- 已完成训练 global step／目标绝对 step：0／5；日志有初始验证和 `epoch 0, step 1`，无更新完成证据。
- 已完成评分题数：初始验证 4 条样本；正式分桶评分未执行。
- 最新完整 checkpoint：无产物证据，未核实目标目录。
- 下一步：将源端 worker 元数据修复同步到目标机，另建 run_id，复跑 5 步并检查完整 checkpoint。

## 可复现信息

- 目标主机：`test-SYS-420GP-TNR`；8×NVIDIA RTX A6000，训练选择 GPU 1、2；检索服务在 8008。
- 目标代码：`/home1/wsh/home/projects/lzy/myprojects/R1/R1`；实际 Git 提交和 dirty 状态未提供。
- 产物：`/home1/wsh/home/projects/lzy/data/search-r1/runs/train-smoke-20260926-112131/train`；日志位于其父目录 `train.log`（目标机未直接读取）。
- 运行配置：Qwen2.5-3B、烟测训练 8 题／开发 4 题、两卡、batch=2、四条轨迹、最大 10 轮、绝对 5 step、检索地址 `http://127.0.0.1:8008/retrieve`、`NCCL_P2P_DISABLE=1`。完整 CLI 见用户粘贴日志。
- 运行环境：目标机迁移检查通过；本次训练已进入 FSDP/vLLM 和真实检索，未完成 GPU 更新。

## 阶段进度与证据

| 检查时间 | 动作 | 实测结果 | 下一步 |
| --- | --- | --- | --- |
| 2026-09-26，用户日志 | 两卡 NCCL 最小通信 | 默认路径和 `NCCL_CUMEM_ENABLE=0` 超时；`NCCL_P2P_DISABLE=1` 双 rank PASS | 目标机训练保持相同通信设置 |
| 2026-09-26，用户日志 | 初始验证与 step 1 生成 | 模型和检索已运行，初始验证 4 样本，step 1 生成完成 | 计算旧策略概率并更新 |
| 2026-09-26，用户日志 | 旧策略 log probability | `dp_actor.compute_log_prob` 缺少 `micro_batch_size`，RayTaskError；未见更新完成 | 同步源端修复并在新输出目录重试 |

本记录不将 `epoch 0, step 1` 记作已完成训练步，也不将 CPU 回归视作 GPU 通过。
