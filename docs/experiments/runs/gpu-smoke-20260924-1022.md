# 实测验证：gpu-smoke-20260924-1022

- 状态：暂停，2026-09-24 10:22 UTC 开始检查，10:38 UTC 因本机 GPU 被其他进程占满而暂停；正式实验步骤：前置短流程验收。
- 执行环境：本机两张 NVIDIA TITAN RTX，各 24 GiB；配置目标 `n_gpus_per_node=2`、`max_turns=10`。
- 目的：用真实下载模型、真实检索资产和少量独立样本，验证评分、更新、完整 checkpoint 及恢复。任何缩小长度、batch 或启用 offload 的设置仅适用于本次烟测。
- 当前已证实：两卡空闲；模型、数据、索引文件存在；8000 端口无检索服务；根分区约 59 GiB、数据卷约 274 GiB 可用。此前 18 个 CPU 测试通过，GPU 全链路未验证。
- 工作区：分支 `data-difficulty`、HEAD `71e072a`、存在未提交和未跟踪实验源码。运行前将在产物目录保存代码差异与未跟踪源码快照及哈希。
- 产物根目录：`/root/data/search-r1/runs/gpu-smoke-20260924-1022`；正式 run_id 未分配，本记录是短流程验证。
- 当前步骤：已验证完整检索与双卡模型初始化；评分首批 rollout 因显存不足失败，调小显存参数的重试尚未进入 rollout；两卡当前无空闲，待资源释放后继续。
- 风险／恢复点：尚未产生标签或 checkpoint。恢复前先确认两卡空闲、检查进程与日志；`run_score.sh` 已改用短 Ray 临时路径与全新 `score-run-offload` 输出目录，旧失败产物保留。

| UTC 时间 | 步骤 | 实际状态及证据 | 下一步 |
| --- | --- | --- | --- |
| 10:22 | 环境预检 | 两卡空闲、数据卷可用，`nvidia-smi`、`df` 与 parquet 元信息检查完成 | 生成小样本并保存代码快照 |
| 10:25 | 小样本准备 | 初次采用 128 token 上限筛选，样本数不足；改用 256 上限，已从原 train split 取训练 8 题、开发 4 题，来源各半且问题去重。产物 `train.parquet`、`dev.parquet`、`sample-manifest.json`；SHA-256 分别见本记录下方 | 启动检索服务 |
| 10:27 | 代码快照 | `tracked.patch` 与 `untracked.tar.gz` 已保存到产物根目录，完整哈希见 `code-sha256.txt` | 启动检索服务 |
| 10:30 | 全量检索加载 | 完整 FAISS 索引和 21,015,324 条语料加载成功；8000 端口服务运行。服务进程约 62 GiB RSS，数据卷剩余约 268 GiB；单查询 3 文档返回成功，首次延迟 22.41 秒。证据 `retriever.log`、本地请求检查。 | 执行两题四轨迹评分 |
| 10:30 | 首次评分失败 | `score.log` 显示 Ray 任务导入 `ray.experimental.state.api` 时，uvloop 的主线程没有事件循环，触发 `RuntimeError`；尚未进入模型加载、未生成标签或 checkpoint。 | 修复 Ray actor 状态查询的导入依赖后重试 |
| 10:31 | GPU 评分开始 | 使用 `run_score.sh`、两张 GPU、`max_turns=10`、真实 3B 模型和真实检索服务；评分只取两题，长度／batch 缩小且 optimizer offload 开启。PID 见 `score.pid`，输出 `score.log`。此时尚无评分结果。 | 检查首批模型加载与显存 |
| 10:32 | 评分重试 | 修复 Ray 导入后，`RayWorkerGroup` 在 uvloop 环境导入成功；两题评分重新启动，PID 见 `score-retry.pid`，日志 `score-retry.log`；模型加载与结果仍待确认。 | 检查加载、推理、标签 |
| 10:33 | 模型加载中 | Ray 导入已通过，两个 WorkerDict 已进入 Qwen2.5-3B/FSDP 初始化，两张卡各占约 5.4 GiB。Ray 报告旧临时目录所在卷 >95% 使用，后续重启可设置数据卷 `RAY_TMPDIR`。此刻尚无标签。 | 等待评分生成、记录故障或结果 |
| 10:34 | 评分重试失败 | 两卡完成 3B/FSDP/vLLM 初始化；首批 rollout 在 vLLM 同步模型权重时 CUDA OOM（GPU 0 仅剩约 23 MiB，需再分配 44 MiB），未生成标签。`score-retry.log` 保留栈。 | 降低本机烟测 vLLM 缓存预算，并启用参数 offload 后重试 |
| 10:35 | 显存调整后评分启动 | `smoke_common.sh` 仅针对本机把 vLLM 显存份额从 0.35 降为 0.20，开启 actor/ref 参数 offload；`run_score.sh` 设置数据卷 `RAY_TMPDIR`。PID 见 `score-offload.pid`，日志 `score-offload.log`；标签未核实。 | 检查首批 rollout、标签产物 |
| 10:36 | 第三次评分启动失败 | Ray 启动时报 UNIX socket 路径超过 107 字节，原因是本次指定的 `RAY_TMPDIR` 位于较深的运行目录；未进入模型加载。证据 `score-offload.log`。 | 改用数据卷短路径后重启 |
| 10:38 | 短路径重试失败 | Ray 在数据卷短路径启动成功，但 `score-run` 是旧显存配置创建的目录，`DifficultyExperiment` 拒绝不同配置复用；尚未加载模型。两张 GPU 此时各被其他不可见进程占用约 23 GiB，无法在本机安全重试。 | GPU 空闲时用新输出目录重启评分 |

- 训练样本 SHA-256：`7a6936902eadc1ce01796608be117254f3781d98f955a902bcd59348b46e35ac`。
- 开发样本 SHA-256：`85538927dc0119d5c0d6397e968973c405934d292855716059e70f2679443114`。
- 代码快照 SHA-256：`tracked.patch` 为 `92978f5a7270805d8b4d9f2153171db74bacef8908d2b98458885e47eb8592ed`；`untracked.tar.gz` 为 `19a610b7bfb6d5a5d264d9a6b63169ff4f3932a2fdbe6bca90720aff33dbd58b`。快照保存于检索启动前；后续代码修改需另存新快照。









## 本次验收结论（10:38 UTC）

真实完整检索服务和单次查询通过；Ray/uvloop 导入故障已修复，双卡 3B/FSDP/vLLM 初始化通过；首次实际 rollout 在 vLLM 权重同步时 CUDA OOM。后续降低缓存和 offload 的参数组合尚未进入 rollout：一次被 Ray socket 路径长度阻断，一次被输出目录配置身份校验阻断。**评分题数 0、训练更新 0 step、完整 checkpoint 0 个、恢复验证 0 次。** 当前两卡被其他进程占满；检索服务已停止以释放资源。下一次应确认 GPU 空闲，并使用 `score-run-offload`、`labels-v0-offload.json` 等新产物路径，保留本次失败记录。所有失败日志在运行产物目录。

- 10:41 UTC 补充检查：轻量 Ray actor 实例的 `ALIVE` 状态查询通过；18 项难度模块 CPU 测试通过（`cpu-tests.log`）；`git diff --check` 通过。端口 8000 已关闭，检索进程已停止。两张 GPU 此时仍由容器外进程占用，各约 23 GiB，未重启评分。

- 10:41 UTC 源码快照：`tracked-final.patch` SHA-256 `682a5e1a20e4f04e00abb33ffc7613bdbcc5fbebeb2800080eebbfca416e65d4`；`untracked-final.tar.gz` SHA-256 `987180eab6134000a8b21a75691748f617dc898ddd6aa5826f14eeb8e9a487b9`，哈希清单 `code-sha256-final.txt`。快照在本条哈希记录写入前生成，源码状态一致。
