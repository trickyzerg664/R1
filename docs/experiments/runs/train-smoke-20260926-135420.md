# 目标机烟测：train-smoke-20260926-135420

## 当前状态

- run_id／实验组／attempt：`train-smoke-20260926-135420`，两卡 5 step 短训练。
- 负责人或 agent 标识：用户在目标机执行；Codex 据用户粘贴的 `train.log` 记录，未直接登录目标机。
- 状态／最后检查时间（UTC）：2026-09-26 14:40，训练 5 step 和最终验证有日志证据；checkpoint 完整性、进程退出码和恢复尚未核实。
- 已完成训练 global step／目标绝对 step：5／5。每步均有 `update_actor` 耗时和 `step:N` 指标。
- 父 checkpoint：无；从 Qwen2.5-3B 初始模型运行。
- 已完成难度评分题数：0；初始、结束验证各报告 `val/num_samples=4`，NQ/HotpotQA 分数均为 0。
- 最新完整 checkpoint：未核实。日志出现 `Saving actor checkpoint to .../step_5/actor`，尚未看到 `COMPLETE.json` 或逐文件哈希校验结果。
- 下一步：目标机读取 `exit_code`，核实 `step_5/COMPLETE.json` 并调用 `read_checkpoint` 校验；随后执行真实恢复烟测。正式 A/B 前还需排查零奖励和大量输出截断。

## 可复现信息

- 分支／代码：用户先前确认目标机 `6693fea`；本次日志没有单独打印 Git 提交或 dirty 状态，实际运行源码身份待目标机核实。
- 启动：用户依据此前完整命令运行，原始命令文本未在此次附件重复；日志解析配置显示 `difficulty_grpo`、两卡、train/val batch=2、PPO mini=8/micro=2、绝对 5 step、test/save_freq=5、vLLM 显存份额=0.35、检索 URL `http://127.0.0.1:8008/retrieve`。shell 环境变量待核实。
- 设备：目标机 `test-SYS-420GP-TNR`，两张 RTX A6000；GPU 1、2 与 `NCCL_P2P_DISABLE=1` 依据此前命令，日志未单独记录设备映射。
- 模型／数据：本地 Qwen2.5-3B、`runs/smoke-input/train.parquet` 和 `dev.parquet`；尚未冻结正式 P/D/T 清单和哈希。
- 产物：`/home1/wsh/home/projects/lzy/data/search-r1/runs/train-smoke-20260926-135420/train`；日志在其父目录 `train.log`，退出码文件预期在父目录 `exit_code`。

## 阶段进度与证据

| 检查时间 UTC | 动作 | 实测进度 | 证据与下一步 |
| --- | --- | --- | --- |
| 2026-09-26 14:40 | 初始化和初始验证 | Qwen/FSDP/vLLM 加载，初始验证 4 样本，NQ/HotpotQA 均 0 | 用户粘贴日志 `step:0` |
| 2026-09-26 14:40 | 训练和更新 | `step:1` 至 `step:5` 均有 `update_actor` 耗时；上次缺温度异常未复现 | 日志 5 行 step 指标；训练更新数 5 |
| 2026-09-26 14:40 | 结束验证和保存 | 结束验证 4 样本，分数均 0；开始写 `step_5/actor` | 检查 `exit_code`、`COMPLETE.json` 和完整性哈希 |

## 验证及结果

- 本次未在目标机执行 checkpoint 目录或恢复检查；不能把保存开始日志视作完整 checkpoint。
- 所有训练步骤显示 `critic/score/mean=0`、`critic/rewards/mean=0`、`critic/advantages/mean=0`，开发集验证也均为 0。可确认流程运行，不能以此判断模型获得了有效学习信号。
- 多数响应达到长度上限（step 3、4 的 `response_length/clip_ratio=1`），并反复出现观察文本超出 128 token 限制的警告。正式实验前应检查答案格式、生成长度和检索观察截断对奖励的影响。
- 运行耗时和 checkpoint I/O 待目标机日志/文件确认；恢复尚未验证。

## 失败、变更与交接历史

| 时间 UTC | attempt／执行者 | 事件及原因 | 保留产物／恢复点 | 后续动作 |
| --- | --- | --- | --- | --- |
| 2026-09-26 14:40 | 用户执行、Codex 据日志记录 | 5 step 与结束验证达到；完整保存与退出码未核实 | `train/checkpoints/step_5`，完整性未知 | 校验 `COMPLETE.json` 和哈希，再做恢复烟测 |
