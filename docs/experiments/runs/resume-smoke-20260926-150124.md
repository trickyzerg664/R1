# 目标机恢复烟测：resume-smoke-20260926-150124

## 当前状态

- run_id／实验组／attempt：`resume-smoke-20260926-150124`，从已校验的 step 5 checkpoint 继续 1 步。
- 负责人或 agent 标识：用户在目标机执行；Codex 据用户终端截图和 `read_checkpoint` 输出记录，未直接登录目标机。
- 状态／最后检查时间（UTC）：2026-09-26 15:29；截图显示 step 6 更新、最终验证完成，进程退出码 0；随后用户运行 `read_checkpoint` 成功，完成标记及逐文件哈希验证通过。
- 已完成训练 global step／目标绝对 step：6／6；本次新增 1 步。没有重复计入父运行的 5 步。
- 父 checkpoint：`/home1/wsh/home/projects/lzy/data/search-r1/runs/train-smoke-20260926-135420/train/checkpoints/step_5`；此前 `read_checkpoint` 哈希校验通过。
- 已完成正式难度评分题数：0；本次最终验证样本数和指标未提供。
- 最新完整 checkpoint：`train/checkpoints/step_6`；`read_checkpoint` 成功并返回 `step=6`，完成标记及逐文件哈希通过。
- 下一步：分析 step 6 奖励、最终验证和答案格式，解决无有效学习信号问题；随后冻结正式题池并开始问题难度打分。正式 A/B 实验仍暂缓。

## 可复现信息

- 分支／代码：用户此前确认目标机有 `6693fea`；本次运行时提交和 dirty 状态未单独记录，未核实。
- 启动：使用上一轮给出的完整恢复命令，`difficulty.resume` 指向父 checkpoint，`resume_mode=continue`、`additional_steps=1`、绝对目标 step 6；GPU 1、2、`NCCL_P2P_DISABLE=1`、两卡、相同数据和检索 ID，检索 URL `http://127.0.0.1:8008/retrieve`。截图不包含解析配置哈希，待核实。
- 模型/reference、数据和检索版本继承父运行；完整资产哈希见父 checkpoint provenance，目标机实际复验仅由运行成功间接支持。
- 产物：`/home1/wsh/home/projects/lzy/data/search-r1/runs/resume-smoke-20260926-150124/train`；日志在父目录 `train.log`，退出码在父目录 `exit_code`。

## 阶段进度与证据

| 检查时间 UTC | 动作 | 状态及实测进度 | 证据与下一步 |
| --- | --- | --- | --- |
| 2026-09-26 15:24 | 父状态恢复和追加训练 | 终端显示 `开始 epoch 0, step 6`、`已完成训练 step 6` | 用户截图；新 checkpoint 待检查 |
| 2026-09-26 15:24 | 保存和最终验证 | 显示 `正在保存 checkpoint`、`最终验证完成`、`退出码: 0` | 后续检查 step 6 完成标记和哈希 |
| 2026-09-26 15:29 | checkpoint 文件校验 | 用户粘贴 `read_checkpoint` 成功输出，`step=6`，与父状态的初始模型/reference、检索 ID、seed、训练指纹和 world size 一致 | 下一步读取 step 6 指标并排查零奖励 |

## 验证及结果

- 本次确认真实训练恢复路径至少完成模型/optimizer 等状态加载并追加一次 actor 更新；随机流和连续运行等价性尚未做数值对照。
- `exit_code=0` 说明训练程序正常结束；随后 `read_checkpoint` 校验了新 checkpoint，但尚未取得最终验证数值或 step 6 奖励，不能据此评价学习效果。
- 正式题池、难度标签、A/B 对照和有效训练信号仍未验收。

## 交接

step 6 完整性已通过。下一步检查 `train.log` 或 `metrics.jsonl` 的 step 6 奖励、响应截断和最终验证；若仍全零，先排查答案格式和配置，再冻结正式实验方案。
