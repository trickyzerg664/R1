# 目标机两题难度评分烟测：score-smoke-20260926-153800

## 当前状态

- run_id／实验组／attempt：`score-smoke-20260926-153800`，烟测 2 题；不是正式 C0/P 评分。
- 负责人或 agent 标识：用户在目标机执行；Codex 据终端截图记录，未直接登录目标机。
- 状态／最后检查时间（UTC）：2026-09-26 16:40；评分进程退出码 0，标签文件读取成功。
- 已完成训练 global step／目标绝对 step：不适用，`difficulty.mode=score` 不更新模型。
- 已完成评分题数／目标题数：2／2；每题显示四个二值奖励，合计 8 条轨迹。
- 最新完整 checkpoint：本次评分不保存训练 checkpoint；此前 step 6 checkpoint 已独立校验。
- 下一步：检查奖励提取、生成格式及截断，取得有区分度的评分条件；再冻结正式题池 P/D/T、统一配置并进行正式 C0 打分。正式 A/B 尚不可开始。

## 可复现信息

- 目标机：`test-SYS-420GP-TNR`；用户依据此前命令选择 GPU 1、2、`NCCL_P2P_DISABLE=1`；本次截图未单独验证硬件映射。
- 输入：`/home1/wsh/home/projects/lzy/data/search-r1/runs/smoke-input/score.parquet`，迁移脚本生成的两题烟测切片；不是正式题池。输出：`/home1/wsh/home/projects/lzy/data/search-r1/runs/score-smoke-20260926-153800/labels-smoke.json`。
- 代码与配置：此前目标机确认 `6693fea`，本次运行时提交/dirty 状态未单独核实；命令使用 `difficulty_grpo`、`difficulty.mode=score`、`score_batch_size=1`、Qwen2.5-3B Base、检索 `http://127.0.0.1:8008/retrieve`、`retrieval_id=wiki18-e5-pinned`、烟测长度与训练配置一致。完整解析配置和哈希未直接提供。
- 日志：`/home1/wsh/home/projects/lzy/data/search-r1/runs/score-smoke-20260926-153800/score.log`；截图确认父目录 `exit_code=0`。标签文件哈希未提供。

## 阶段进度与证据

| 检查时间 UTC | 动作 | 实测结果 | 证据与下一步 |
| --- | --- | --- | --- |
| 2026-09-26 16:40 | Ray 与评分轨迹生成 | 启动成功，终端两次显示正在生成评分轨迹 | 用户截图；查看完整日志可获得细节 |
| 2026-09-26 16:40 | 标签保存与读取 | 进程退出码 0；读取到 2 条记录，每条四个奖励且均为 0，`k=0` | 用户截图；下一步诊断生成与奖励 |

## 验证及结果

- 两题的四条奖励均为 `[0.0, 0.0, 0.0, 0.0]`；两个标签均为 `k=0`，对应 H 桶。这只证明当前小切片的完整评分路径可用，不能代表正式题池分布。
- 当前没有 M/E 桶，无法基于这两题运行预定的 H/M/E 配比。此前训练 5 步奖励和开发集分数也为 0，且生成结果频繁达到长度上限并出现无效动作。
- QA 奖励使用严格 EM：从完整 prompt+响应中取最后一个 `<answer>...</answer>`；当前 prompt 含示例答案标签，代码要求至少两个答案标签才接受模型输出。输出未包含新答案标签或答案错误都会得 0。本次没有完整逐轨迹生成文本，尚不能确定两题全错的具体原因，不能仅凭零奖励认定代码错误。Codex 在源机同源原始 parquet 首题上做了受控 CPU 检查：把正确答案按 `<answer>...</answer>` 附在原 prompt 后，`compute_score_em` 返回 1.0；无新答案标签返回 0.0。该检查只证明一个标准格式输入可正常计分，不代表目标机八条生成轨迹的内容。
- 正式 P/D/T、标签 `labels_v0`、桶分布与计算成本均未形成。训练步骤和 checkpoint 本次不适用。

## 交接

先读取这次标签中的 `generated_tokens`、`search_queries`，结合 `score.log` 查答案格式与截断。修正生成条件或模型起点须在新的运行目录重做评分及训练烟测，并在正式对照组统一冻结。不要将 `labels-smoke.json` 当作正式标签输入。
